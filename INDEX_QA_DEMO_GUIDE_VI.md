# Hướng dẫn mở bản QA Demo v4.5.60

- Dùng `index.html` cho Demo tiêu chuẩn.
- Dùng `index-qa-demo-v4.5.60.html` cho dữ liệu QA phức tạp.
- Không nhập dữ liệu thật hoặc bí mật vào bản Demo.
- Chạy bằng backend (`npm start`) hoặc một static server; không mở trực tiếp bằng `file://` khi cần kiểm tra PWA/Service Worker.
