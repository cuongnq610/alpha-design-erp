# Edge Functions

`invite-user` tạo lời mời Supabase Auth bằng service role ở phía máy chủ và gán người dùng vào công ty. Không đưa service-role key vào frontend.

Triển khai bằng Supabase CLI:

```bash
supabase functions deploy invite-user
```
